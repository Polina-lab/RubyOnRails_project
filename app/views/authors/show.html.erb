<h1><%= full_name(@author)%></h1>

<p>Email: <%= @author.email %></p>

<span>
    <strong>Associated articles: </strong>
    <%= check_output(@author.id)%>
</span>
<ul>
    <% check_articles(@author.id).each do |article|%>
        <li>
            <%= link_to article.title, article %>
        </li>
    <% end %>
</ul>
    
        

<p>Other actions: </p>

<ul>
  <li><%= link_to "Edit", edit_author_path(@author) %></li>
  <li><%= link_to "Destroy", author_path(@author), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<%= link_to "Back to Articles", articles_path %>
